export interface ProjectProps {
    title: string;
    description: string;
    tags: string[];
    imageURL: string;
    gitHubURL: string;
    projectURL: string;
}

export interface BlogProps {
    title: string;
    description: string;
    link: string;
}